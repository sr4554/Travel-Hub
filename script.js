// Script to handle potential image loading delay (optional)
window.onload = function() {
    const botContainer = document.getElementById('bot-container');
    botContainer.style.opacity = 1; // Make the icon visible after loading
  };
  